import 'package:flutter/material.dart';

class ChallengesContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Challenges/Tasks Screen', style: TextStyle(color: Colors.white, fontSize: 18)),
    );
  }
}
